/**
 * @param {string[]} strs
 * @return {string[][]}
 */
var groupAnagrams = function (strs) {
    if (strs.length === 1) return [strs]
    const ans = []

    let temp = [strs[0]]
    for (let i = 0; i < strs.length - 1; i++) {
        if (){}
    }
};